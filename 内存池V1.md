# 内存池V1

## 1. 内存池介绍

### 1.1 什么是内存池？

内存池是一种**预分配内存并进行重复利用**的技术，通过**减少频繁的动态内存分配**与**释放操作**，从而提高程序运行效率。

内存池通常**预先分配一块大的内存区域**，将其**划分为多个小块**，将这块内存的管理放在应用层执行，每次需要分配内存时**直接从这块区域中分配**，而不是调用系统的动态分配函数（如`new`或`malloc`），减少**系统调用**带来的开销。

### 1.2 内存池的优点

**减少动态内存分配的开销**：系统调用`malloc/new`和`free/delete`涉及**复杂的内存管理操作**（如内存查找、碎片整理），导致性能较低，而内存池通过**预分配**和**简单的管理逻辑**显著提高了分配和释放的效率。

**降低系统调用频率**：系统级内存分配（如`malloc`）需要进入**内核态**，频繁调用会有较高的性能开销。内存池通过减少系统调用频率提高程序效率。

**避免内存碎片**：动态分配内存会产生内存碎片，尤其在**大量小对象**频繁分配和释放的场景中，导致的后果就是：当程序长时间运行时，由于所申请的内存块的大小不定，频繁使用时会**造成大量的内存碎片**从而降低程序和操作系统的性能。内存池通过**管理固定大小的内存块**，可以有效**避免碎片化**。

### 1.3 内存池的缺点

需要**预先分配较大的内存区域**，可能浪费一些内存

**不适合大型对象**



## 2. 什么是内存碎片？

造成堆内存利用率很低的一个主要原因就是**内存碎片化**。内存碎片化就是计算机程序在运行过程中，**频繁地内存分配与释放引起的内存空间不连续性问题**，可能导致内存利用率降低甚至无法分配所需的内存。内存碎片主要分为**内碎片**和**外碎片**两种类型。

### 2.1 内碎片

- **定义**：内碎片指已分配的内存块未被实际使用的部分。即程序请求的内存小于分配的内存块大小时，多余的部分形成内碎片。
- **产生原因**：内存分配器通常按固定的对齐规则分配内存块（如对齐到 4 字节或 8 字节），分配大小往往是申请大小的倍数。
- **举例**：程序需要 13 字节内存，但内存分配器按 16 字节对齐规则分配了 16 字节。**多出的 3 字节就是内碎片**。

------

### 2.2 外碎片

- **定义**：外碎片是指**系统中有足够总量的空闲内存**，但这些**空闲内存不连续**，无法满足一个**较大的分配**请求。
- **产生原因**：频繁的小内存块的分配和释放导致**内存分布变得零散**，和不连续的小块空闲内存无法自动组合成足够大的连续块。
- **举例**：系统中有多个小块空闲内存，总量为 100MB，但是由于这些空闲内存块彼此不连续，无法分配一个需要 50MB 的大块。

------

**注**：内存池的固定大小块分配等机制，可以减少有效外碎片，内存池的内存分配策略根据实际需求制定的越精细产生的内碎片越少，**内存碎片是不可避免只能减少的**。



## 3. 内存池的框架——基于哈希映射的多种定长内存分配器

![image-20260717142945561](C:\Users\28783\AppData\Roaming\Typora\typora-user-images\image-20260717142945561.png)

**本质：预先向系统申请一块大的内存，把它交付给应用层管理，在程序运行时，内存池的分配和回收都由应用层的内存池处理，减少系统调用**

### 3.1 内存池框架核心逻辑：

主要框架如上图所示，维护一个**哈希桶 `MemoryPools`**，里面**每项对应一个内存池 `MemoryPool`**，哈希桶中每个内存池的块大小 **`BlockSize` 是相同的**（4096 字节），但是每个内存池里每个块分割的大小**`SlotSize` 是不同的**，依次为 8,16,32,…,512 字节（需要的内存**超过 512 字节就用 `new/malloc`**），用户申请不同大小的内存就**通过哈希桶的映射找到相应（槽大小）`SlotSize` 的内存池向其申请**，比如用户分别申请 8 字节和 12 字节的内存，则经过哈希函数的计算找到槽大小为 8 字节的内存池和槽大小为 16 字节的内存池分别分配内存给用户，这样设置的好处是**可以保证内存碎片在可控范围内**。

### 3.2 问为什么用户申请超过 512 字节的内存就直接调用 `new/malloc` 等系统调用？

**答**：因为内存池主要解决的是**小内存带来的内存碎片问题**和**小内存频繁申请释放带来的性能问题**。但如果是用户申请较大块的内存依然选择通过内存池分配内存则会**导致内存池频繁因为管理的内存不足而向系统申请内存**，这样既没有起到性能优化的作用同时增加了程序的复杂度。

![image-20260717143916718](C:\Users\28783\AppData\Roaming\Typora\typora-user-images\image-20260717143916718.png)

#### (1) 管理层变量 (MemoryPool 结构体内部)

这部分变量记录全局状态：

1.  **`SlotSize_` (8B)**
    *   **含义**：每个内存槽的大小。
    *   **作用**：定义了用户**每次申请内存的单位**。
    *   **注意**：`SlotSize` 必须大于等于指针的大小。因为当 Slot 被释放后，我们需要在 Slot 内部存一个指针（指向下一个空闲槽），所有大小至少为8字节。

2.  **`BlockSize_` (4096B)**
    *   **含义**：内存池向系统一次性申请的大块内存大小。
    *   **作用**：减少系统调用。图中设为 4096 字节，里面包含了管理信息和多个连续的 Slot。

3.  **`ptr to firstBlock` (对应文中 `currentBlock`)**
    *   **含义**：指向内存池申请的**第一个**内存块的指针。
    *   **作用**：内存池通过这个指针管理所有申请过的 Block。所有的 Block 形成一个链表，通过这个**头指针**，在**内存池销毁时可以遍历并释放所有向系统申请的内存**，防止内存泄漏。

4.  **`ptr to freeSlot` (对应文中 `freeSlot`)**
    *   **含义**：指向**已释放槽位链表**的头指针。
    *   **作用**：这是内存池**回收机制**的核心。当用户调用 `delete/free` 时，**内存并不还给操作系统，而是串成一个链表（list of freed slots），由该指针维护。**
    *   **分配优先级**：**最高**。**申请内存时，先看 `freeSlot` 是否为空，如果不为空，直接从中取出一个给用户。**

5.  **`ptr to curSlot` (对应文中 `currentSlot`)**
    *   **含义**：指向当前 Block 中**从未被使用过**的**第一个 Slot 的指针**。
    *   **作用**：这是内存池**线性分配**的游标。
    *   **分配优先级**：次之。如果 `freeSlot` 为空，说明没有回收回来的内存，就从 `currentSlot` 指向的地方切一块给用户，然后 `currentSlot` 后移。

---

#### (2) 区块层变量 (Block 内部)

Block 是内存池向系统申请来的原始内存。

1.  **`ptr to nextBlock`**
    *   **含义**：指向下一个 Block 的指针。
    *   **作用**：将所有申请的大块内存串联起来。当一个 Block 用完后，内存池会申请新 Block，并用这个指针把它们连成链表。

2.  **`pad area` (填充区)**
    *   **含义**：字节对齐所需的填充空间。
    *   **作用**：保证后面的 Slot 能够按照 8 字节对齐，提高 CPU 读写效率。

3.  **`ptr to lastSlot`**
    *   **含义**：指向当前 Block **最后一个可用位置的边界指针**。
    *   **作用**：**用于判断当前 Block 是否已经用完**。如果 `currentSlot` 到达了 `lastSlot`，说明这个 Block 没空间了，需要向系统申请新的 Block。

---

#### (3) 槽位层变量 (Slot 内部及其状态)

这是最终交给用户使用的最小单元。

1.  **`used slot` (已使用槽位)**
    *   里面存储的是**用户放入的真实数据。**

2.  **`freed slot` (已释放槽位)**
    *   **核心逻辑**：当 Slot 被释放后，它内部的 8 字节空间会被**强制类型转换**为一个指针（`ptr to next free slot`），指向下一个被释放的槽位。
    *   这就是图中顶部 **"list of freed slots"** 的来源。

3.  **`unused slot` (未使用槽位)**
    *   纯净的、还没分配给任何人的内存，**处于 `currentSlot` 之后的区域。**

---

#### (4) 核心流程串联

当用户调用 `allocate()` 申请内存时：

1.  **检查 `freeSlot`**：
    *   如果有值：说明有之前回收的内存。把 `freeSlot` 指向的那个 Slot 给用户，**并把 `freeSlot` 更新为该 Slot 内部存的下一个地址。**
2.  **检查 `currentSlot`**：
    *   如果 `freeSlot` 是空的，回收的内存为空。**看 `currentSlot` 是否还没到 `lastSlot`。**
    *   没到：把 `currentSlot` 指向的内存给用户，然后 `currentSlot += SlotSize`。
3.  **申请新 Block**：
    *   如果 `currentSlot` 也到头了，**说明该Block已被完全使用完**。**向系统申请一个新的 `BlockSize` 大小的 Block**，更新 `firstBlock` 链表，把 `currentSlot` 指向新 Block 的第一个槽位



## 4. 内存池核心接口提取

该项目对外提供了两个重要的接口 `newElement` 和 `deleteElement`，分别是向内存池申请内存和将申请的内存进行回收操作。

### 4.1 `newElement` 函数

```cpp
template<typename T, typename... Args>
T* newElement(Args&&... args);
```

**执行逻辑**：

用户在构造对象 T 时调用 `newElement<T>()` 申请内存。该方法会先计算元素 T 的大小**并传入哈希桶**，哈希桶根据**哈希映射选择对应槽大小的内存池进行分配**：

1. 优先使用 `FreeList_` 里的空闲内存槽
2. 其次查找 `curSlot_`，若 `curSlot_` 大于等于 `lastSlot_`，则重新申请一块内存块来分配内存槽
3. 获得内存后，**在该内存上构造对象 T** 并返回地址给用户

### 4.2 `deleteElement `函数

```cpp
template<typename T>
void deleteElement(T* p);
```

**执行逻辑**：

用户释放对象时调用 `deleteElement` 函数：

1. 先调用对象 T 的析构函数
2. 再通过哈希映射找到对应的内存池，将该内存槽加入到 `FreeList_` 中



## 5. 涉及相关知识

### 5.1 原子指针Slot

```cpp
struct Slot 
{
    // Slot* next;
    std::atomic<Slot*> next; // 原子指针
};
```

给 `Slot* next` 加上 `std::atomic`（原子性）是为了实现**线程安全**，为了实现**无锁并发访问**。

#### 5.1.1 防止竞态条件

内存池的 `freeSlot` 链表是一个共享资源。如下场景：

*   **线程 A** 正在执行 `deallocate`（释放内存），试图把一个 Slot 插回链表头部。
*   **线程 B** 同时也执行 `deallocate`，也想把另一个 Slot 插回链表头部。

**如果没有 atomic：**

1. 线程 A 读取了当前的头指针 `Head`。
2. 线程 B 也读取了同样的头指针 `Head`。
3. 线程 A 把自己的 `next` 指向 `Head`，并把 `Head` 改成自己。
4. 线程 B 把自己的 `next` 也指向 `Head`，并把 `Head` 改成自己。
5. **结果：** 线程 A 刚刚归还的内存块“丢失”了，链表结构被破坏，造成内存泄漏。

#### 5.2.2 无锁比加锁快得多

要解决上面的问题，有两种办法：

*   **办法 A（加锁）：** 在操作链表前 `mutex.lock()`，操作完 `unlock()`。
    *   *缺点：* **锁很重**。高并发下，几十个线程排队等一把锁，内存池会变成系统的性能瓶颈。
*   **办法 B（原子操作）：** 使用 `std::atomic` 
    *   *优点：* 它是硬件级别的指令，**不需要让线程进入内核态挂起**。如果写入失败，**它会立刻重试**。这种“无锁”设计能极大地提高高并发下的吞吐量。



### 5.2 申请释放内存的逻辑

#### 第一层：用户接口层
**核心函数：** `newElement<T>` / `deleteElement<T>`
**职责：** 处理“对象生命周期”，完成**“生内存”**与“C++对象”之间的转换。

*   **申请逻辑 (`newElement`)**：
    1.  **计算大小**：获取 `sizeof(T)`。
    2.  **获取生内存**：调用下层 `HashBucket::useMemory` **拿到 `void*` 指针**，用`reinterpret_cast`强转为`T`类型指针
    3.  **构造对象**：使用 **Placement New** 在该地址上**调用 `T` 的构造函数**，支持完美转发（`Args&&...`）。
*   **释放逻辑 (`deleteElement`)**：
    1.  **析构对象**：显式**调用 `p->~T()`**，执行对象自身的清理工作。
    2.  **回收内存**：x先强转为`void*`类型，然后调用下层 `HashBucket::freeMemory` **将内存还给池子**。

```cpp
template<typename T, typename... Args>
T* newElement(Args&&... args)
{
    T* p = nullptr;
    p = reinterpret_cast<T*>(HashBucket::useMemory(sizeof(T)));
    if (p != nullptr)
        // 执行构造函数
        new(p) T(std::forward<Args>(args)...);
    return p;
}

template<typename T>
void deleteElement(T* p){
    if (p){
        p->~T();
        HashBucket::freeMemory(reinterpret_cast<void*>(p), sizeof(T));
    }
}
```

#### 5.2.1 类型的静态重解释（reinterpret_cast）

```cpp
p = reinterpret_cast<T*>(HashBucket::useMemory(sizeof(T)));
```

* **技术定义**：`reinterpret_cast `是 C++ 的强制类型转换符，用于处理**无关类型之间的转换**。

- **内存表现**：该操作不修改内存数据或位模式。它仅在编译器符号表中将 `useMemory `返回的 `void*` 地址重新标记为 `T*` 类型。
- **必要性**：`void*` 指针无法进行成员访问、偏移计算或析构函数调用。转换后，指针 p 获得了**根据 T 类型定义的内存布局进行寻址的能力**。

#### 5.2.2 定位构造（Placement New）与 完美转发

```cpp
new(p) T(std::forward<Args>(args)...);
```

- **定位 New 机制**：
  - 标准 new 包含**“内存分配”**和**“对象构造”**两个阶段。
  - **new(p)** 语法**仅触发“对象构造”阶段**。它直接**接收预分配的地址 p**，在该地址处**调用 T 的构造函数**。
- **完美转发**：
  - `Args&&... args`：利用**万能引用**接收任意数量和类型的参数。
  - `std::forward<Args>(args)...`：保留参数的原始值类别（左值或右值）。若用户传入右值，则触发 T 的移动构造函数而非拷贝构造函数，减少内存开销和计算周期。

#### 5.2.3 显式析构调用

```cpp
p->~T();
```

- **生命周期解耦**：在**自定义内存管理**中，对象的生命周期不再由**作用域**或**标准 delete 控制**。
- **操作定义**：**直接通过指针调用析构函数**。此操作仅执行析构函数体内的逻辑（如释放 T 内部持有的其他堆资源、关闭文件描述符等），而不会释放 p 指向的内存空间。
- **顺序要求**：必须**先执行析构函数**，待对象状态清理完毕后，**再将该内存地址返回**给 `MemoryPool `的 `freeList_`。

#### 5.2.4 原始内存回收

```cpp
HashBucket::freeMemory(reinterpret_cast<void*>(p), sizeof(T));
```

- **类型擦除**：将 `T*` 重新转换回 `void*`，丢弃类型信息，进入无类型内存处理流程。
- **分发逻辑**：`freeMemory `根据 `sizeof(T`) 查找对应的 `MemoryPool `实例。
- **归还机制**：调用 `MemoryPool::deallocate`

#### 第二层：路由分发层 

**核心函数：** `useMemory` / `freeMemory` / `getMemoryPool`
**职责：** 根据内存大小决定去向。

*   **大对象**：若 `size > 512B`，则**跳过内存池**，**直接使用系统原生的 `operator new/delete`**。
*   **小对象映射**：若 `size <= 512B`，则通过**哈希公式计算索引**。
    *   **计算公式**：`index = ((size + 7) / 8) - 1`。这保证了请求会映射到最贴近且 $\ge$ 请求大小的 8 字节倍数池子中。
*   **单例池管理**：通过 `static MemoryPool memoryPool[64]` 维护 **64 个规格不同的内存池实例**，**每个实例只负责一种固定大小**。

```cpp
MemoryPool& HashBucket::getMemoryPool(int index)
{
    static MemoryPool memoryPool[MEMORY_POOL_NUM];
    return memoryPool[index];
}

static void* useMemory(size_t size)
{
    if (size <= 0)
        return nullptr;
    if (size > MAX_SLOT_SIZE) // 大于512字节的内存，则使用new
        return operator new(size);

    // 拿到合适的内存池，然后执行该内存池申请内存的函数
    return getMemoryPool(((size + 7) / SLOT_BASE_SIZE) - 1).allocate();
}

static void freeMemory(void* ptr, size_t size)
{
    if (!ptr)
        return;
    if (size > MAX_SLOT_SIZE)
    {
        operator delete(ptr);
        return;
    }
    // 拿到合适的内存池，然后执行该内存池释放内存的函数
    getMemoryPool(((size + 7) / SLOT_BASE_SIZE) - 1).deallocate(ptr);
}
```

#### 5.2.5 规格对齐与索引映射逻辑

```cpp
getMemoryPool(((size + 7) / SLOT_BASE_SIZE) - 1) 
```

- **对齐原理**：为了实现向上取整，通常使用 (size + (align - 1)) / align。
- **计算过程示例**：
  - 若 size = 1：(1 + 7) / 8 = 1。减 1 后索引为 **0**（对应 8B 池子）。
  - 若 size = 8：(8 + 7) / 8 = 1。减 1 后索引为 **0**（对应 8B 池子）。
  - 若 size = 9：(9 + 7) / 8 = 2。减 1 后索引为 **1**（对应 16B 池子）。
- **设计目的**：
  1. **防止溢出**：确保分配的内存块（Slot）永远不小于请求的 size。
  2. **减少碎片**：通过 8 字节步进的分级方式，将内部碎片控制在 7 字节以内。

#### 5.2.6 系统分配函数

当 s`ize > MAX_SLOT_SIZE（512字节）`时，代码转而调用系统原生接口。

```cpp
operator new(size);

operator delete(ptr);
```

- **技术定义**：
  - 这里的` operator new `是**分配函数**，而非 new 表达式。
  - **区别**：`new T() `会触发**内存分配**和**构造函数**，而 `operator new(size)` 仅负责从堆中**申请指定字节数的原始内存**，**返回类型为 `void*`**。
- **调用原因**：
  1. **接口一致性**：`MemoryPool::allocate() `返回的是 `void*`（原始内存）。使用 `operator new `可以保证在**大对象分支下返回相同性质的原始内存**，使得上层 `newElement `能够统一处理。
  2. **性能边界**：内存池的优势在于处理海量小对象。对于超过 512B 的大对象，系统 malloc 的管理开销占比降低，且大对象在池中容易造成空间浪费，因此直接交由操作系统的堆管理器处理更具效率。
- **无析构语义**：
  - `operator delete(ptr) `与` free() `**等价**，它**仅将内存块归还给系统堆**，不会检查或调用该地址上可能存在的对象的析构函数。这符合分发层的职责——仅处理空间，**不干涉对象生命周期**。



#### 第三层：核心执行层

**核心函数：** `allocate` / `deallocate` / `push/popFreeList`
**职责：** 处理“并发安全”与“物理内存分配”。

此层级采用了两套不同的并发策略，兼顾了**高性能**与**线程安全**：

##### 1. 无锁优先级（处理 Freed Slots）

为了追求极致性能，对于“回收后再利用”的逻辑采用了**无锁（Lock-free）**设计：
*   **`freeList_`**：是一个原子指针 `std::atomic<Slot*>`。
*   **操作方式**：使用 `compare_exchange_weak` (CAS) 实现无锁压栈和弹栈。
*   **优势**：当多个线程频繁申请和归还相同大小的对象时，它们直接竞争原子变量，不需要阻塞等待，吞吐量极高。

```cpp
void* MemoryPool::allocate()
{
    // 优先使用空闲链表中的内存槽
    Slot* slot = popFreeList();
    if (slot != nullptr)
        return slot;
    
    Slot* temp;
    {   
        std::lock_guard<std::mutex> lock(mutexForBlock_);
        if (curSlot_ >= lastSlot_){
            allocateNewBlock();
        }
        temp = curSlot_;
        // 块右移一个
        curSlot_ += SlotSize_ / sizeof(Slot);
    }
    return temp; 
}

void MemoryPool::deallocate(void* ptr)
{
    if (!ptr) return;
    Slot* slot = reinterpret_cast<Slot*>(ptr);
    pushFreeList(slot);
}

// 无锁入队操作
bool MemoryPool::pushFreeList(Slot* slot)
{
    while (true)
    {
        // 获取当前头节点
        Slot* oldHead = freeList_.load(std::memory_order_relaxed);
        // 将新节点的 next 指向当前头节点
        slot->next.store(oldHead, std::memory_order_relaxed);
        // 尝试将新节点设置为头节点
        if (freeList_.compare_exchange_weak(oldHead, slot,
         std::memory_order_release, std::memory_order_relaxed))
        {
            return true;
        }
        // 失败：说明另一个线程可能已经修改了 freeList_
        // CAS 失败则重试
    }
}

// 无锁出队操作
Slot* MemoryPool::popFreeList()
{
    while (true)
    {
        Slot* oldHead = freeList_.load(std::memory_order_acquire);
        if (oldHead == nullptr)
            return nullptr; // 队列为空

        // 在访问 newHead 之前再次验证 oldHead 的有效性
        Slot* newHead = nullptr;
        try
        {
            newHead = oldHead->next.load(std::memory_order_relaxed);
        }
        catch(...)
        {
            // 如果返回失败，则continue重新尝试申请内存
            continue;
        }
        
        // 尝试更新头结点
        // 原子性地尝试将 freeList_ 从 oldHead 更新为 newHead
        if (freeList_.compare_exchange_weak(oldHead, newHead,
         std::memory_order_acquire, std::memory_order_relaxed))
        {
            return oldHead;
        }
        // 失败：说明另一个线程可能已经修改了 freeList_
        // CAS 失败则重试
    }
}
```

#### 5.2.7 指针步进计算

```cpp
curSlot_ += SlotSize_ / sizeof(Slot);
```

该表达式用于**在当前内存块**（Block）内移动**分配游标**。

*   **指针算术规则**：在 C++ 中，对类型为 `T*` 的指针进行 `ptr + n` 操作，其实际移动的内存字节数为 $n \times sizeof(T)$。
*   （目的是移动n，但默认还要*sizeof(T)，那就在默认基础上再除以sizeof(T)就好了）
*   **计算逻辑**：
    *   `curSlot_` 的类型是 `Slot*`。
    *   **目标是使指针向后移动物理长度为 `SlotSize_` 的字节。**
    *   因此，必须将字节偏移量 `SlotSize_` 换算为以 `sizeof(Slot)` 为单位的步进值。
*   **更新结果**：执行后，地址精确向后偏移了 `SlotSize_` 字节，指向下一个物理槽位的起始位置。

#### 5.2.8 原子加载与内存序：`load(std::memory_order_relaxed)`

`load` 操作用于**从原子变量中读取当前值**。

*   **`std::memory_order_relaxed`**：
    *   **定义**：松散内存序。它仅保证当前读取操作是**原子性**的（不会读到被拆分的中间地址），但不保证不同 CPU 核心之间的指令执行顺序。
    *   **应用意图**：在 `pushFreeList` 的开始阶段，获取 `oldHead` 只是为了后续 CAS 操作做准备。即便此时读到的是稍旧的值，后续的 `compare_exchange_weak` 也会因为地址不匹配而失败并重试，因此无需高成本的同步开销。
*   **`oldHead->next.load(...)`**：由于 `Slot` 结构体中的 `next` 也是 **`std::atomic`，必须使用 `load` 获取其指向的下一个空闲节点地址**。

#### 5.2.9 原子存储：`store(..., std::memory_order_relaxed)`

```cpp
slot->next.store(oldHead, std::memory_order_relaxed);
```

*   **语义**：**将当前链表的头指针 `oldHead` 写入新 Slot 的 `next` 成员中。**
*   **为何使用 Relaxed**：此时 `slot` 指向的内存刚由用户通过 `deallocate` 归还，且尚未通过 `freeList_` 对外公开，属于该线程的私有操作。只要保证写入地址的完整性即可，同步工作将由下一步的 CAS 操作完成。

#### 5.2.10 CAS 操作：`compare_exchange_weak`

这是实现无锁逻辑的核心指令，即“比较并交换”。

**1. 入队操作（Push）：**
```cpp
freeList_.compare_exchange_weak(oldHead, slot, std::memory_order_release, std::memory_order_relaxed)
```
*   **逻辑**：检查 `freeList_` 是否仍等于 `oldHead`。
    *   **若相等**：说明期间没有其他线程修改链表头，则将 `freeList_` 更新为 `slot`。
    *   **若不等**：说明发生了竞争，更新 `oldHead` 为最新值并返回 `false`（触发循环重试）。
*   **`memory_order_release`（成功序）**：确保在此之前的写入操作（如 `slot->next` 的赋值）对后续执行 `acquire` 读取的线程可见。这建立了一个“同步点”。
*   **`weak` 的含义**：允许在值相等的情况下产生“虚假失败”，但在循环结构中性能优于 `strong` 版本。

**2. 出队操作（Pop）：**

```cpp
freeList_.compare_exchange_weak(oldHead, newHead, std::memory_order_acquire, std::memory_order_relaxed)
```
*   **逻辑**：尝试将链表头从 `oldHead` 切换到其下一个节点 `newHead`。
*   **`memory_order_acquire`（成功序）**：与 `release` 配对。确保当前线程读取到的 `newHead` 及其指向的内存内容是完整的，防止 CPU 乱序执行导致读到未初始化的节点数据。

### 5.3 内存块开辟与对齐逻辑——**用头插法**

当 `freeList_` 为空，且当前内存块（Block）也用完时，需要开辟新块：

*   **`mutexForBlock_`**：为了防止多个线程同时发现内存不够而导致重复向系统申请昂贵的 Block 空间，这里使用了 `std::lock_guard`。
*   **`allocateNewBlock`**：向系统申请 4096B 的大块内存，并将其通过头插法挂载到 `firstBlock_` 链表（用于最后析构时整体释放）。
*   **指针对齐**：使用 `padPointer` 确保分配出的每个 Slot 都是严格对齐的，提高 CPU 读写效率。

```cpp
void MemoryPool::allocateNewBlock()
{   
    void* newBlock = operator new(BlockSize_);
    reinterpret_cast<Slot*>(newBlock)->next = firstBlock_;
    firstBlock_ = reinterpret_cast<Slot*>(newBlock);

    char* body = reinterpret_cast<char*>(newBlock) + sizeof(Slot*);
    size_t paddingSize = padPointer(body, SlotSize_);
    curSlot_ = reinterpret_cast<Slot*>(body + paddingSize);

    lastSlot_ = reinterpret_cast<Slot*>(reinterpret_cast<size_t>(newBlock) + BlockSize_ - SlotSize_ + 1);
}

// 让指针对齐到槽大小的倍数位置
size_t MemoryPool::padPointer(char* p, size_t align)
{
    // align 是槽大小
    size_t rem = (reinterpret_cast<size_t>(p) % align);
    return rem == 0 ? 0 : (align - rem);
}
```

#### 5.4.1 Block 链表管理（头插法）

```cpp
void* newBlock = operator new(BlockSize_);
reinterpret_cast<Slot*>(newBlock)->next = firstBlock_;
firstBlock_ = reinterpret_cast<Slot*>(newBlock);
```

*   **空间申请**：通过 `operator new` 申请 `BlockSize_`（通常为 4096 字节）的原始内存。
*   **管理信息嵌入**：每个 Block 的**前 8 个字节**被强制转换为 `Slot*` 指针。
*   **链表维护**：利用这 8 个字节存储 `firstBlock_` 的旧值，然后更新 `firstBlock_` 指向新块。这一步将所有向系统申请的 Block 串联成链表，确保析构时能够遍历并释放。

#### 5.4.2 内存偏移与物理布局计算

```cpp
char* body = reinterpret_cast<char*>(newBlock) + sizeof(Slot*);
size_t paddingSize = padPointer(body, SlotSize_);
curSlot_ = reinterpret_cast<Slot*>(body + paddingSize);
```

*   **跳过管理区**：`body` 指针通过 `+ sizeof(Slot*)` 跳过了 Block 开头的链表指针区域。
*   **计算对齐填充（Padding）**：
    *   调用 `padPointer` 计算从 `body` 地址开始到符合 `SlotSize_` 对齐要求的地址之间需要填充的字节数。
    *   **对齐意义**：现代 CPU 访问对齐到特定倍数（如 8 或 16 字节）的内存地址时速度最快。如果 Slot 起始地址不对齐，可能导致跨缓存行（Cache Line）访问，降低性能。
*   **设置分配游标**：`curSlot_` 指向该 Block 中第一个可分配槽位的精确起始地址。

#### 5.4.3 分配终止边界计算（`lastSlot_`）

```cpp
lastSlot_ = reinterpret_cast<Slot*>(reinterpret_cast<size_t>(newBlock) + BlockSize_ - SlotSize_ + 1);
```

该公式用于确定当前 Block 能够分配出的**最后一个合法槽位的边界**。

*   **计算逻辑**：
    *   `reinterpret_cast<size_t>(newBlock) + BlockSize_`：定位到该 Block 内存区域的绝对末尾。
    *   `- SlotSize_`：回退一个槽位的大小。如果游标超过此位置，说明剩余空间不足以存放一个完整的对象。
    *   **`+ 1` 的作用**：配合 `allocate()` 中的判断条件 `if (curSlot_ >= lastSlot_)`。当 `curSlot_` 刚好等于最后一个合法 Slot 的起始地址时，`curSlot_ < lastSlot_` 为真（因为加了 1），允许分配最后一个 Slot。一旦分配后指针再次累加，就会触发扩容。

#### 5.4.4 对齐偏移计算函数

```cpp
size_t MemoryPool::padPointer(char* p, size_t align)
{
    size_t rem = (reinterpret_cast<size_t>(p) % align);
    return rem == 0 ? 0 : (align - rem);
}
```

*   **取模运算（`%`）**：`rem` 代表当前地址 `p` 相对于对齐边界 `align` 的偏移量。
*   **补齐逻辑**：
    *   若 `rem == 0`，说明地址已经对齐，无需填充。
    *   若不为 0，则返回 `align - rem`。例如：地址为 10，要求 8 对齐。`10 % 8 = 2`，则需要填充 `8 - 2 = 6` 字节，使地址达到 16（8 的倍数）。

---

### 5.5 Block 物理内存布局图示

一个完整初始化后的 Block 内部结构如下：

| 字节偏移                  | 数据内容                    | 作用说明                                  |
| :------------------------ | :-------------------------- | :---------------------------------------- |
| **0 ~ 7**                 | `nextBlock` 指针            | 指向老 Block 的地址，用于管理链表         |
| **8 ~ (8 + padding)**     | `Padding`                   | 字节对齐填充，确保后续 Slot 性能          |
| **(起始游标 `curSlot_`)** | **Slot 1 (SlotSize_ 字节)** | 第一个可分配的槽位                        |
| **...**                   | **Slot 2...n**              | 后续连续的内存槽位                        |
| **(边界 `lastSlot_`)**    | **剩余空间 / 警戒位**       | 空间不足时，游标到达此处触发新 Block 申请 |